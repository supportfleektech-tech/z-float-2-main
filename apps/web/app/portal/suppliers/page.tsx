"use client"; import { useEffect, useState } from "react";
import { PageHeader, Button, Input, Label, TableShell, Badge, Skeleton, Modal, Spinner } from "@/components/ui"; interface Supplier { id: string; name: string; phone: string | null; email: string | null; type: string; status: string;
} export default function SuppliersPage() { const [rows, setRows] = useState<Supplier[] | null>(null); const [open, setOpen] = useState(false); const [form, setForm] = useState({ name: "", phone: "", email: "" }); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null); function load() { fetch("/api/recipients") .then((r) => r.json()) .then((d) => setRows((d.data ?? []).filter((r: Supplier) => r.type === "supplier"))) .catch(() => setRows([])); } useEffect(load, []); async function create(e: React.FormEvent) { e.preventDefault(); setBusy(true); setError(null); const res = await fetch("/api/recipients", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, type: "supplier" }) }); const data = await res.json(); setBusy(false); if (!res.ok) { setError(data.error?.message ?? "Failed to add supplier"); return; } setOpen(false); setForm({ name: "", phone: "", email: "" }); load(); } return ( <div> <PageHeader title="Suppliers & vendors" subtitle="Verified payees with a payment history you can reconcile." actions={<Button onClick={() => setOpen(true)}>+ Add supplier</Button>} /> {!rows ? ( <div className="space-y-3">{[...Array(4)].map((_, i) => <Skeleton key={i} className="h-12" />)}</div> ) : ( <TableShell headers={["Supplier", "Phone", "Email", "Status"]}> {rows.map((s) => ( <tr key={s.id} className="hover:bg-surface/60"> <td className="px-4 py-3 font-medium">{s.name}</td> <td className="px-4 py-3 font-mono text-xs">{s.phone ?? "—"}</td> <td className="px-4 py-3 text-sm text-muted">{s.email ?? "—"}</td> <td className="px-4 py-3"><Badge tone={s.status === "ACTIVE" ? "success" : "warning"}>{s.status === "PENDING" ? "Pending approval" : s.status}</Badge></td> </tr> ))} </TableShell> )} <Modal open={open} onClose={() => setOpen(false)} title="Add supplier"> <form onSubmit={create} className="space-y-4"> <div> <Label>Supplier name</Label> <Input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /> </div> <div> <Label>Phone</Label> <Input required inputMode="tel" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} /> </div> <div> <Label>Email (optional)</Label> <Input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /> </div> {error ? <p className="text-sm text-danger">{error}</p> : null} <div className="flex justify-end gap-2"> <Button type="button" variant="secondary" onClick={() => setOpen(false)}>Cancel</Button> <Button type="submit" disabled={busy}>{busy ? <Spinner className="h-4 w-4 text-white" /> : "Save supplier"}</Button> </div> </form> </Modal> </div> );
}
