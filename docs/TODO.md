# TODO

Working backlog — ordered by impact. Check off as completed.

> **Suite-status note (2026-09-04):** older completed items below record the
> Playwright baseline as "33 passed + 1 flaky retry-pass" — that was the state at
> the time. The flake-elimination close-out (KNOWN_LIMITATIONS #23) removed the
> rotating flake at the root: the suite is now **34/34 passed**, `data/e2e/final-suite-*.log`.

## Short-term (demo hardening)

- [x] Add GET endpoints for batches: `GET /api/batches` and `GET /api/batches/{id}` (BigInt-safe via `toJsonSafe`), tenant-scoped. *(done — live verified: list + detail with rows)*
- [x] Idempotent payment replay returns the original payment's current state instead of re-submitting (`replayed` flag honored in POST /api/payments). *(done — live verified: same key → same payment, no double execution)*
- [x] Route integration tests to `DATABASE_URL_TEST` (`IS_TEST` set in the shared test setup) so suites can never truncate the dev/live DB. *(done — suites run sequentially via `test:unit`/`test:integration`)*
- [x] Deterministic E2E on the production server: `next start` + build env + global-setup warmup. *(done — 15/15 in ~15s: marketing, portal, admin + RBAC, Tier-1 (payment-links public pay, webhook subscribe UI, statement import, notifications bell, MFA enroll→backup-codes→disable), and compliance: KYC upload→scan→submit + watchlist-hit payment→case review)*
- [x] Approvals center renders batch name + total for batch approval requests (API exposes `batchId`/`batch`). *(done)*
- [x] `pnpm db:reset` restored (drop → migrate → seed; runbook RB-01). *(done)*
- [x] Full local gate script: `pnpm run ci:local` (`ci` is a pnpm built-in). *(done — final run green: lint, typecheck 0 errors, 119 unit + 70 integration (incl. notifications 6, kyc 18, Daraja contract 10), web prod build 90/90)*
- [x] Wire full cron parsing for `CUSTOM` schedules (cron-parser in payments-core; unparseable expressions fall back to +7 days). *(done — 7 unit tests)*
- [x] Add CSV export to reports (reports table + `reports.generate` worker job writes tenant-scoped CSV to `REPORT_STORAGE_DIR`; `POST/GET /api/reports` + `GET /api/reports/{id}/export`; portal Reports page has Export buttons + download list; XLSX still phase 2). *(done — live verified incl. cross-tenant 404/401)*
- [x] Verify `bank-psp` in provider registry or remove the bank webhook route. *(done — registered and exercised live: received / duplicate / 400 on bad signature)*
- [x] Ledger health job: `checkLedgerHealth` (ledger package) verifies per-journal balance + net-zero debits/credits; worker housekeeping runs it at startup and every 10 min, writing `ledger.health.imbalance` audit events. *(done — 3 integration tests)*
- [x] Email the invite token in `/api/team` instead of returning it (feature-flag the demo return). *(done — POST returns no token/link; EMAIL notification carries it in production, IN_APP in demo; raw token only ever stored in demo (`raw_token`, migration 009) and surfaced via the invite-notice bridge; E2E 1/1)*

## Tier-1 (landed)

- [x] **TOTP MFA end-to-end** — enroll (secret + otpauth URI + QR), verify, disable (password + current code), 8 single-use backup codes, login challenge; RFC-6238 vector tests green. *(done — live verified)*
- [x] **Payment links with public pay page** — `pl_…` tokens, public `/pay/{token}` page + collect API, funds merchant wallet via balanced journal, payerRef idempotent replay, maxUses/expiry caps. *(done — live verified incl. replay + limit rejection)*
- [x] **Outbound tenant webhooks** — subscribe CRUD (`/api/webhooks`), outbox-driven fanout, HMAC-SHA256 `x-zfloat-signature` over the exact bytes POSTed (jsonb ordering fixed via `payload_body`), 2s→30s retries ×5, delivery log. *(done — live verified: 200 DELIVERED + signature verified; dead endpoint FAILED after 5 attempts)*
- [x] **Statement-import reconciliation** — CSV import (`provider_reference,amount,date`) → matching runs with MATCHED/PARTIAL/UNMATCHED + AMOUNT_MISMATCH/UNMATCHED exceptions surfaced in `/api/reconciliation` + portal page. *(done — live verified)*
- [x] **In-app notifications read path** — `GET/POST /api/notifications` (tenant-scoped, unread count, mark-read) + portal bell in layout header. *(done — live verified)*
- [x] **Out-of-band channels (Tier-2)** — `notifications.send` worker dispatch with recipient fallback (`data.to/phone` → users row); EMAIL drivers `sink|smtp|console`, SMS drivers `http` (Africa's Talking-compatible wire: `POST {url}` Bearer `SMS_API_KEY`, `{to:[E.164], from, message}`) `|console`; Message Center UI (`/portal/messages`) with per-channel history/status + test-send. *(done — live verified: EMAIL JSON in `data/mailbox`, AT-shaped POST captured by gateway mock at :9090, rows QUEUED→SENT with `driver`; unit 6/6)*
- [x] **KYC / AML engine (Tier-2)** — `@zfloat/kyc`: deterministic watchlist screening (token/Jaccard scoring, alias support, inactive entries), screening rows + review-case workflow with risk levels, document intake gated on the `files.scan` malware pipeline, profile submit→review decision, payments gate (`screenRecipientForPayment` in `submitPayment`: watchlist hit ⇒ `aml_watchlist_hit` risk flag + forced PENDING_APPROVAL even with no policy rules), admin compliance workspace (`/admin/kyc`, `admin.kyc` permission), portal compliance page, audit events for every decision. *(done — unit/integration 18/18; live API + Playwright E2E 2/2)*
- [x] **Daraja (M-Pesa) adapter contract tests** — `MpesaProviderAdapter` exercised against a local Daraja emulator speaking the documented wire protocol: OAuth client-credentials auth + token caching, B2C `BusinessPayment`, STK `CustomerPayBillOnline`, transaction-status query, reversal; ResponseCode mapping, timeout ≠ failure, fail-closed without credentials, production base-URL override refused. *(done — 10/10; `MPESA_API_BASE_URL` test-only override)*

## Medium-term

- [ ] Live provider certification: point `MpesaProviderAdapter` (contract-tested 10/10 vs emulator) at Daraja sandbox/live with real shortcode + credentials, then PesaLink, RTGS/EFT bank files.
- [x] **Object storage driver (`@zfloat/storage`)** — `local` + `s3` behind one interface; backs KYC evidence uploads and report exports; the `s3` driver is contract-tested 8/8 against a real MinIO server (put/get/head/delete, presigned URLs, tenant key prefixes, traversal refusal) and was live-verified through the product (worker log `store=s3`, DB `payload_ref=s3://reports/{id}.csv`, KYC objects listed in the bucket; binary at `scripts/tools/minio`). *(done — unit 8/8; E2E 15/15 green on the local driver)*
- [x] **Developer API portal (`/portal/developers`)** — tenant API keys (`zf_live_…`; SHA-256 hashes only, raw shown once, revocable, optional expiry, `last_used_at`), public API v1 (`/api/public/v1/payments` POST/GET + `/wallets`) with Bearer/x-api-key auth, tenant isolation, 60 req/min rate limit, idempotency-key replays (no double execution), consistent error codes; create/revoke audited. *(done — auth integration 7/7, live curl verified, E2E 1/1: create→pay→replay→revoke→401)*
- [x] **PWA / installability** — web app manifest (`/manifest.webmanifest`: name, display standalone, 192+512+maskable icons, `#0F5BFF` theme), Apple/theme metadata, generated brand icons, own service worker `/sw.js` (offline app-shell fallback, stale-while-revalidate static assets, never intercepts `/api/*`), registered on the production build. *(done — web typecheck 0 errors, prod build 95/95, E2E 1/1: manifest served with required fields, icons 200 image/png, SW registers → activated, offline reload renders cached shell)*
- [x] **Observability & alerting (`/admin/health`)** — worker heartbeat written to Redis every 30s; ledger double-entry health summary persisted per run; health API v2 returns dependency checks (postgres/redis/worker/outbox/ledger), live metrics (critical-queue backlogs, payments & webhook deliveries 24h, outbox lag, active API keys, heartbeat age) and evaluated alert rules via the new dependency-free `@zfloat/observability` package (pure `evaluateAlerts` + shared thresholds; UI auto-refreshes every 30s). *(done — observability unit 24/24 incl. boundary thresholds; web typecheck 0, prod build 95/95, E2E 18/18 single run; live: 12 rules evaluated, worker heartbeat fresh, all checks OK, 0 critical — WARN correctly fires on real 24h webhook-delivery failures)*
- [ ] AWS S3 production config: bucket policies, versioning/lifecycle, SSE-KMS, IAM roles; validate against real S3 (MinIO-verified so far).
- [ ] Watchlist data: replace demo entries with a licensed provider feed (the seam is the `aml_watchlists` table).
- [ ] Delivery receipts + templates for email (SMTP/SES) and SMS (Africa's Talking / Twilio) with real credentials.
- [x] Payment-link branded checkout page + hosted-link management UX polish (copy, share, QR on portal). *(done — per-row Share modal: full URL + copy, WhatsApp/email share links, tenant-scoped QR PNG endpoint `/api/payment-links/:id/qr`; E2E 1/1)*
- [x] Statement/report scheduling (monthly exports to S3 with retention). *(done — `report_schedules` (migration 010): monthly/weekly dispatch on a 60s worker tick produces normal reports rows → existing generate pipeline → object store; retention sweep purges artifacts past `retention_days` incl. store delete; API + portal Scheduled exports panel; integration 5/5, E2E 1/1, live: due schedule exported 77 rows and advanced next run)*
- [x] Admin console: biller catalog editor, airtime catalog editor, approval-policy builder UI. *(done — `/admin/catalogs` (biller + airtime/data CRUD) and `/admin/policies` (tenant picker, ordered rule builder, pause/activate, versioned v1→v2 edits); catalog mutations are now maker-checked (staged → 202 → second platform admin approves in `/admin/approvals`; audit before/after events written on apply by the checker — see the #14b entries above); the approval-policy builder console is maker-checked too since slice 2 (create/pause/activate/rule edits staged → ops approves); Batch-B E2E 5/5)*
- [x] Audit log viewer improvements: before/after diff rendering, filters by actor/resource. *(done — `GET /api/admin/audit` filters action (ilike)/resourceType/resourceId/actor email/from/to with limit≤500 and returns `before`/`after` payloads + `ipAddress`; `/admin/audit` rows expand into ADDED/REMOVED/CHANGED field diffs with struck-through old values, populated resource-type select, actor filter and event/payload counts; Batch-B E2E covers API + UI)*

- [x] Platform-admin maker-checker **slice 2** (GAP-ANALYSIS Phase 1): pricing/fee-rule changes, approval-policy publish/pause/version edits and feature-flag toggles now stage behind the SAME `platform_config` engine + config approvals centre (kinds `fee_rule | approval_policy | feature_flag`). POST /api/admin/pricing (create or update by `ruleId`), POST /api/admin/flags, POST + PATCH /api/admin/policies[/id] answer 202 `PENDING_APPROVAL`; nothing is written until a second platform admin approves. Apply cores preserve direct-route semantics under the approver's authority: fee-rule version bumps + `fee_versions` snapshots (`pricing.rule.updated` with before/after), policy rule edits archive live rules to `approval_policy_versions` and bump v→v+1 (`approval.policy.update`), pause/activate flip `active` with distinct audits (no version bump), flag toggles audit `feature_flag.updated`; rule changes are detected at apply time via rule signatures so queued changes never clobber each other. Consoles show staged-submission notices and never optimistically lie about row state; the approvals centre renders the new kinds generically. *(done — approvals integration suite 25/25 incl. fee create/update-merge/versioning, policy canonicalization (KES major → minor strings), archive v1→v2, pause/activate, flag toggle, unsupported-op + tenant pre-checks; full gate: lint 0, typecheck 0, unit 212, integration 113, web prod build, Playwright 30/30 (1 flaky retry-pass) incl. the two-person policy-builder E2E (stage → ops approve → v1; pause; activate; v2 + archived v1))*

- [x] **Webhook reliability ops (GAP-ANALYSIS Phase 2).** Outbound tenant webhooks: `webhook_deliveries` is now an explicit DLQ — migration 015 adds `last_error` (payload bytes/attempts/HTTP status were already stored per delivery) and `webhook_subscriptions.secret_version/secret_rotated_at`. `deliverWebhook` classifies failures (HTTP 4xx → permanent FAILED on the DLQ with the reason; 5xx/network → exponential backoff 2s→30s max 5 with `nextRetryAt` + `last_error`; exact signed bytes persisted on every outcome). APIs: `POST /api/webhooks/deliveries/:id/replay` (re-enqueue FAILED, guarded on subscription ACTIVE, audited `webhook.delivery.replayed`) and `POST /api/webhooks/:id/rotate-secret` (fresh secret, version bump, audited `webhook.secret.rotated`, shown once — regeneration semantics documented: old secret invalid immediately, later attempts re-sign with the new one). Portal `/portal/webhooks`: Failed/DLQ filter tab with counts, per-row Inspect modal (payload, signed bytes, signature, last error), Replay action, and Rotate-secret per endpoint with v1/v2 badges. *(done — payments-core integration +10 on real PG (webhook-reliability suite: 200 path, 400-permanent no-retry, 5xx ladder→DLQ, network error text, disabled-sub, replay guards + re-sign, rotation bump + old-secret rejection, rotated-then-replayed end-to-end); E2E +1 on the production build (reject → DLQ → inspect → rotate v2 → replay → DELIVERED, HMAC-verified against only the new secret); full gate: lint 0, typecheck 0, unit 222, integration 123, web prod build, Playwright 31/31 (1 flaky retry-pass))*

- [x] **Reconciliation exception resolution workflow (GAP-ANALYSIS Phase 3).** Recon exceptions (AMOUNT_MISMATCH/UNMATCHED/DUPLICATE/UNKNOWN/ORPHAN) gain an auditable in-app lifecycle: migration 016 adds append-only `recon_exception_activity`; `packages/payments-core/src/recon-exceptions.ts` implements `resolveReconException` (required resolution note; OPEN/INVESTIGATING/ESCALATED → RESOLVED, stamps actor/time), `reopenReconException` (RESOLVED → OPEN; row-level resolution cleared, history keeps it) and `commentOnReconException` (any status); every action is recorded in the activity table AND the audit log (`reconciliation.exception.resolved|reopened|commented`) with before/after status. APIs: GET `/api/reconciliation/exceptions/:id` (detail + actor-resolved history), POST `/resolve` `/reopen` `/comment` — tenant-scoped for members (`reconciliation.manage` to act), cross-tenant for platform admins; GET `/api/reconciliation?scope=all` now returns every tenant's exceptions with tenant names. Portal and admin (`/admin/reconciliation`) rows open a workflow panel with context, inline actions and the full activity history; CSV import + run buttons preserved. *(done — payments-core integration +9 on real PG (recon-workflow: resolve stamps/activity/audit, comment keeps state, reopen preserves history, detail/list joins + filters, note/status/cross-tenant guards); E2E +2 on the production build (portal comment→resolve→reopen with actor history; admin cross-tenant view + platform comment); full gate: lint 0, typecheck 0, unit 231, integration 132 (payments-core 70), web prod build, Playwright 33/33 (32 passed + 1 known-flaky retry-pass))*
- [x] **Alert delivery (GAP-ANALYSIS Phase 4).** Out-of-band ops alerting on /admin/health: pure rule evaluation
  in `@zfloat/observability` (worker_stale, payment_failure_rate, queue_backlog, ledger_imbalance…) fed by live
  checks (PG/Redis/worker/outbox/ledger), severity rollup, and per-rule out-of-band dispatch via the notification
  pipeline (`apps/web/lib/ops-alerts.ts`: OPS_ALERT_EMAILS/SMS recipients, per-rule Redis cooldown, every send a
  persisted `ops.alert` notification row); /admin/health renders checks, metrics, evaluated rules and an Alert
  delivery panel (QUEUED→SENT) with a Send-test-alert button. *(done — E2E on the production build: test alert
  travels the real queue→worker→EMAIL path to SENT; final-gate Playwright 33 passed + 1 flaky retry-pass)*
- [x] **DSAR operations (GAP-ANALYSIS Phase 5).** Admin export + erasure endpoints backed by
  `packages/audit/src/dsar.ts`: export = personal-data bundle (financial records referenced, not dumped; immutable
  login/security events counted); erasure = identity scrub to `erased-<id>@erased.invalid`, delete of operational
  stores (sessions/MFA/roles/notifications/invitations), retention of ledger/payments/recon + append-only audit
  rows under legal obligation, `dsar.erasure.executed` audit trail, confirm `"ERASE"`, self-erasure 403.
  `docs/compliance/odpc-dpa-pack.md` §4 rewritten around the endpoints. *(done — dsar.integration 4/4 in the final
  gate, incl. deleted-vs-retained assertions; HTTP mapping 404/409/422/403/400)*
- [x] **Backup automation + restore drill (GAP-ANALYSIS Phase 6).** `scripts/backup.sh` (pg_dump -Fc + sha256 +
  `KEEP_DAYS` 7d retention, cron `0 2 * * *`) and `scripts/restore-drill.sh` (sha256 verify → restore newest dump
  to a scratch DB → 12-table row parity → smoke query → drop scratch; evidence log per run). *(done — final-gate
  reruns: `data/backups/zfloat-20260903-030220.dump` + drill log 12/12 SUCCESS; WAL-archiving guidance added to
  runbook RB-05)*
- [x] **Ops hardening small (GAP-ANALYSIS Phase 7).** Per-queue explicit BullMQ retry policies (`QUEUE_POLICIES` +
  `resolveJobOptions` in `@zfloat/queue`; attempts/backoff per queue — spec 3/3) and an admin-auth Prometheus
  `/api/admin/metrics` endpoint (queue gauges per state, outbox lag, worker heartbeat age, PG/Redis up, 24h
  payment/webhook totals, ledger run age; scrape config documented in runbook RB-09b). *(done — type-checked,
  prod-build-verified, exercised live by the soak runner and E2E suite)*
- [x] **Public API docs (GAP-ANALYSIS Phase 8).** `apps/web/lib/public-api-spec.ts` builds the OpenAPI 3.1 document
  (single code truth for the public surface) served at `/api/public/v1/openapi.json` with a portal Developers-page
  link; `scripts/verify-openapi.mjs` curl matrix proves every documented path against the live API. *(done — final
  live run 7/7 PASS; evidence `data/openapi-verify-1788405172219.json`)*
- [x] **Load/soak evidence (GAP-ANALYSIS Phase 9).** `scripts/soak/run.mjs` — bounded Node profiles of concurrent
  public-API payment creates (multi-key round-robin under the documented 60/min/key guard), waits for queue+outbox
  drain via /metrics, writes latency percentiles + status breakdown. *(done — live runs in `data/soak/`: 120@10
  (p50 168/p95 310ms), 200@20 (p50 318/p95 527ms), final clean-state 120@10 (p50 153/p95 271ms); 0 rejected across
  clean runs; 380/380 soak payments SUCCESS in DB; drain 0/0. Observations, not benchmark claims — see caveats
  (per-key limiter trip run + E2E webhook-fixture residue) in GAP-ANALYSIS Phase 9)*
- [x] **External seams design docs (GAP-ANALYSIS Phase 10, doc-only by design).** `docs/seams/`: Daraja statement
  pull for recon (adapter call shape), disputes & chargebacks (schema proposal + open product questions),
  partial refunds & fee handling (ledger semantics), eTIMS/VAT + WHT (requirements + adapter seam), payroll
  statutory PAYE/NSSF/SHIF (needs authoritative dated rates source). No fake live integrations — credentials/
  product decisions are the documented blockers. Multi-currency stays explicitly deferred (KES-only launch).



- [x] Multi-region Postgres + read replicas; outbox poller as separate deployment. *(done — outbox poller extracted to the shared `runOutboxPoller` in `packages/payments-core/src/outbox-relay.ts`: Postgres advisory-lock lease arbitration (`pg_try_advisory_xact_lock`, key 723993001) so worker + N standalone relays coexist with exactly-one dispatch per tick; embedded in the worker (unchanged 5s cadence) and deployable standalone as the new `services/outbox-relay` (`pnpm dev:relay` / `start:relay`, `OUTBOX_RELAY_INTERVAL_MS`); integration 5/5 incl. lease contention, plus live proof (relay + worker both up → single dispatch, marked published once); multi-region/read-replica topology rules documented in runbooks RB-12/RB-13; full gate: unit 174, integration 86, E2E 25/25)*
- [x] KMS-backed secret management instead of env vars. *(done — new `@zfloat/secrets` vault: platform secrets (`SESSION_SECRET`, `ENCRYPTION_KEY`, M-Pesa/B2C creds, S3 keys, SMTP/SMS/bank/airtime keys) resolve env-var-first, then AES-256-GCM envelope files at `SECRETS_DIR/<NAME>.json` sealed via `SECRETS_DRIVER` — `local` (dev/staging 64-hex key) or `kms` (AWS KMS-wrapped per-secret data keys, real envelope encryption); applied once at startup by `applySecretsToEnv()` — worker + standalone relay (fail-closed on vault errors) and web (`lib/secret-guard.ts` awaited at `requireUser`/rate-limit/kyc/webhook chokepoints before the memoized `getConfig` fills its env snapshot); CLI `pnpm secrets gen-key|set|unset|list` (mode-600 files, plaintext never persisted, rotation = re-seal, KMS key re-wrap is zero-downtime); `.env.example` + runbook RB-14; unit 11/11 incl. tamper rejection, env-override precedence, insecure-default replacement, kms no-envelope no-op; live CLI round-trip verified (seal → list → apply, file contains no plaintext); KMS driver live proof deferred to when real AWS credentials are supplied)*
- [x] Per-wallet reservation ledger to parallelize concurrent funding. *(done — new append-only `wallet_ledger_entries` table (custom migration 011 + drizzle schema) with DB-enforced immutability, per-type delta semantics and balance-after == wallet-state guard; every reserve/release/apply/fund and every DENIED double-spend attempt is recorded in-transaction by `@zfloat/ledger` (reserveFunds writes the denied entry in its own tx so the audit row survives the InsufficientFundsError); crediting paths (`fundWallet`, `collectViaPaymentLink`) now record FUND entries; APIs: `listWalletLedgerEntries`, `countDeniedReservations`, `recordWalletFunding`; ledger integration 19/19 incl. parallel-reservation arbitration with exactly one winner + denied audit + append-only + tamper-guard tests; live: E2E flows wrote 84 ledger rows (44 RESERVE / 39 APPLY / 1 FUND) with wallet available/reserved consistent; full gate: unit 192, integration 93, E2E 25/25)*
- [x] Idempotency garbage collection for keys older than retention policy. *(done — `gcIdempotencyRecords`: frees IN_PROGRESS keys after 24h, purges finished records after 90d; worker hourly; integration 6/6)*
- [x] SOC2-style evidence collection; pen-test runbook; ODPC DPA compliance pack. *(done — `scripts/compliance/collect-evidence.ts` (`pnpm evidence`): pulls audit/login/security events, access review (users+roles+MFA), consent/DSAR/KYC records, ops health, integrity-migration ledger, redacted env snapshot (secret values hashed, never written), dependency audit into `data/compliance/<ts>/` with SHA-256 MANIFEST chain of custody; docs: `docs/compliance/README.md`, `soc2-controls-map.md` (TSC → evidence), `odpc-dpa-pack.md` (records of processing, DPA terms, DSAR 30-day procedure, 72h breach notification), `pen-test-runbook.md` (scope, OWASP→guard mapping, financial double-spend tests, reporting/SLA); runbooks RB-16/17/18; live run verified: 12 artifacts, 88 audit + 43 security rows, no secret plaintext, ledger 84 rows consistent)*
- [x] **Maker-checker hardening across sensitive resources.** Payments/batches already enforced the two-person rule; this pass extends it to every single-actor execution that could move funds or grant power: (1) **payee book** — adding a payee registers it PENDING; an independent approver's sign-off activates it before funds can move to it (`assertPayeeApproved` refuses payments referencing non-active payees); (2) **reversals** — requesting one only raises an approval request (payment stays put); a checker's approval executes the provider reversal + compensating journal (`executeApprovedReversal`, idempotent); (3) **privileged role invites** — inviting into roles that carry approve/reverse/manage powers leaves the invite PENDING until a checker approves; registration is refused (`ROLE_AWAITING_APPROVAL`) before that; (4) **submit-path policy resolution** — routes now resolve the tenant's ACTIVE published policy instead of hard-wiring an empty rule set that silently disabled the gate: demo mode uses the built-in KES-10,000 threshold policy, production **fails closed** (`APPROVAL_POLICY_REQUIRED`) when no policy is published; (5) Approval center renders payments/batches/payees/reversals/role-grants and the maker≠checker rule holds everywhere (creator cannot approve own request, even via delegation). Schema: migration 012 (`approval_requests.resource_type/resource_id`, `invitations.role_approval_status/role_approved_by_id`) + 013 (schema-drift repair `webhook_events.payload_body`). *(done — integration +7 (payee/reversal gates) and +3 (invite gate) on real PG; E2E +4 on the production build (above-threshold payment → approver release; payee PENDING → ACTIVE; reversal request → REVERSED; role invite pre-approval 403 → approve → 201); full gate: typecheck 0 errors, unit/integration/web suites green (integration incl. 10 new maker-checker gate tests), Playwright 28 passed + 1 pre-existing flaky (retry-pass); live curl-verified on the demo stack)*
- [x] **Platform-admin maker-checker for catalog configuration (KNOWN_LIMITATIONS #14b slice 1).** New `platform_config` approval resource in `@zfloat/approvals` (`packages/approvals/src/platform-config.ts`): biller/airtime catalog POST/PATCH/DELETE routes now stage a change (`createPlatformConfigChange`) and answer 202 `{requestId, status:PENDING_APPROVAL}` — nothing is written until a second platform admin approves (`decidePlatformConfigChange`); maker ≠ checker enforced by the engine (self-approval → `MAKER_CHECKER_VIOLATION`); validation mirrors the old direct-write routes (code immutability with full-form echo tolerance, CODE_EXISTS pre-check at staging); apply-time conflicts (duplicate race) never throw — recorded `applied:false` + `execution_error` + `platform.config.apply_failed` audit. Decision centre `GET/POST /api/admin/config-requests[/id]` (SUPER_ADMIN role gate; audit written on the approver incl. new-row resource ids) and the admin config approvals centre UI (`/admin/approvals`: PENDING/ALL/APPROVED/REJECTED tabs, proposed-change + before snapshots, decision history, executionError surface); catalogs console banners the staged submission and the maker's own requests carry no action buttons. Migration 014 (`approval_requests.executed_at/execution_error`); seed creates the second platform admin `ops@zfloat.app` (idempotent, `OPS_ADMIN_EMAIL`-overridable). *(done — approvals integration suite 25/25 (slice-1 races/code-echo + slice-2 fee/policy/flag kinds; see slice-2 item); web prod build clean; batch-b E2E 5/5 (two-person CRUD + policy builder via centre UI; audit events authored by the checker); full Playwright run 30/30 (1 flaky retry-pass on the invite spec); live curl proof: maker stages biller → checker approves → row appears; self-approve 403; reject path, update, delete verified; gate: lint 0, typecheck 0, unit 212, integration 113)*
