{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && (pnpm -r --no-bail --filter './packages/*' build || true) && pnpm -r --filter './packages/*' build && pnpm --filter @zfloat/worker build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @zfloat/worker start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
