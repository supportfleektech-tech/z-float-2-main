[phases.setup]
nixPkgs = ["nodejs", "corepack"]

[phases.install]
cmds = [
  "corepack enable pnpm",
  "pnpm install --frozen-lockfile",
]

[phases.build]
cmds = [
  "pnpm --filter @zfloat/outbox-relay build",
]

[phases.deploy]
cmd = "pnpm --filter @zfloat/outbox-relay start"